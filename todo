playlist buttons which starts playing that in shuffle
repeat/shuffle button

use local playing media metadata to show youtube video etc playing
shift the music up around the currently playing alternative so it looks like its an interruption

timers:
render active timer under the date
activate commands like: timer 30m
other command such as timer cancel

mouse gestures generic across programs


bugs:
text on the weather widget top needs outline so it doesnt blend into clouds
outline still doesnt work perfectly for example for M

tracks:
2026-08-24T11:04:48.568408Z ERROR cantus::app::music::spotify::playlists: Failed to update Spotify library error=Client specified an invalid argument { Response status code: 405 Method Not Allowed } track_id=5a6p56me3eFdKXcWnsPtLE

lyrics:
if a song doesnt have lyrics the previous songs always shows on the left without moving
empty space between tracks displays no lrics when it should just count as a large space
the lyric to word could be a lot better and simpler, the variable speed

do a pass through everything, cantus and isthmus, identify cruft and legacy stuff to remove, identify places where we arent using immediate mode to its fullest potential, identify some radical suggestions to improve both the backend and frontend of isthmus/cantus


have the paint output give you the options for blending? and then it auto handles premultiply etc? custom Rgba struct instead of vec4?

replace isthmus build with cargo gpu

blur behind playhead?
