playlist buttons which starts playing that in shuffle
repeat/shuffle button

use local playing media metadata to show youtube video etc playing
shift the music up around the currently playing alternative so it looks like its an interruption

timers:
render active timer under the date
activate commands like: timer 30m
other command such as timer cancel

mouse gestures generic across programs

web demo:
spotify demo
paries as wallpaper in the demo

bring back the edge sheen

https://iquilezles.org/articles/distfunctions2d/
no more RoundedTriangle or RoundedRect shape, use simple sdf function to round
maybe shapes shouldnt be individual structs? but Shape itself is more generic and holds the boolean operations in order to compute bounds etc? So you construct the shape from an original shape then apply operations to it
perhaps auto compute bounds with quadtree or find interior point and iterate out to find extremes?
perhaps view where we need bounds for sdfs see if we actually need that at all?


do a MASSIVE refactor pass throughout, do every audit you can imagine for code quality
performance, deduplication, inlining single use small functions/variables in ways that doesnt hurt readability, consistency, reduce unnecessary wrapper functions, reduced reexports, correctness, idiomatic rust pass, reduce overengineering/complexity, simplify away boilerplate code, increase usage of immediate mode, find areas lightweight helper crates can reduce our codebase complexity, reduce struct redestructuring or variable redefinitions that aren't necessary, specified lifetimes and drop() etc can be examples of where improvement is possible (though not for certain a problem), feature gates are considered as discouraged as much as possible, clean up/simplify comments to one line max with no comments that read as changelogs, unify platform code as much as possible and also target code wherever we dont truly need branching or feature gates, compile time errors not runtime, renaming variables/functions to better represent what they do
major focus on LOC reduction, remove legacy cruft, we exclusively target modern systems and features with no legacy support
genuine simplifications, radical suggestions including deep refactors
reevaluate it all from first principles always, greenfield treat it as a fresh project where nothing is off limits, keep it simple and elegant
research heavily, find similar projects to aspects of our codebase to learn from their strengths

for cantus + isthmus
